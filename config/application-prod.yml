server:
  port: 8080
spring:
  application:
    name: servico-file-config
service-parametro:
  parametro1: "prod"
  parametro2: "prod"
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
