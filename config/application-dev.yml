
server:
  port: 8080
spring:
  application:
    name: servico-file-config
service-parametro:
  parametro1: "desenvolvimento"
  parametro2: "dev"
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
