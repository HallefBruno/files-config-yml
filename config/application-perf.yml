
server:
  port: 8080
spring:
  application:
    name: servico-file-config
service-parametro:
  parametro1: "perf"
  parametro2: "perf"
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
