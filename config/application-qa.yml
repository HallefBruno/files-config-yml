   
server:
  port: 8080
spring:
  application:
    name: servico-file-config
service-parametro:
  parametro1: "qa"
  parametro2: "qa"
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
